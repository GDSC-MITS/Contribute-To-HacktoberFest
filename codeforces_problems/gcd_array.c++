#include <bits/stdc++.h>
#include<iostream>
using namespace std;
int main()
{
int t;
cin>>t;
while(t--)
{
    int l,r,k;
    cin>>l>>r>>k;
    vector<int> v;
    for(int i=l;l<=r;++i)
    {
        v.push_back(i);
    }
    sort(v.begin(),v.end());
    if(v[0]%2==0)
    {
        for(int i=1;i<v.size();++i)
        {
            6y
        }
    }
}

return 0;
}